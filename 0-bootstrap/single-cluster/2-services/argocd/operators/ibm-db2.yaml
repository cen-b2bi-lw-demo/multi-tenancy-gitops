apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ibm-db2ucluster
  annotations:
    argocd.argoproj.io/sync-wave: "225"
  labels:
    gitops.tier.group: db2
    gitops.tier.layer: services
    gitops.tier.source: git
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: db2
    server: https://kubernetes.default.svc
  project: services
  source:
    repoURL: "" # Populated by kustomize patches in 2-services/kustomization.yaml
    targetRevision: "" # Populated by kustomize patches in 2-services/kustomization.yaml
    path: instances/ibm-sfg-db2ucluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true